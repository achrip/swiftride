import SwiftUI

struct ContentView: View {

    var body: some View {
        ZStack {
            MapView()

            //SearchDrawer()

            //DetailDrawer()

            //RoutesDrawer()
        }
    }
}
