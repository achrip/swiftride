import SwiftUI

struct ContentView: View {

    var body: some View {
        ZStack {
            MapView()

            //SearchDrawer(showFavoritesView: .constant(true))

            //DetailsDrawer()

            //RoutesDrawer()
        }
    }
}

#Preview {
    ContentView()
}
